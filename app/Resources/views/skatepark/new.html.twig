{% extends 'base.html.twig' %}

{% block body %}
<div class="web-down">
    <div class="well admin">
    <h1>Skatepark creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

        <a href="{{ path('skatepark_index') }}">Back to the list</a>
    </div></div>
{% endblock %}
